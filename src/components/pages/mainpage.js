import React, { Component } from "react";
import { Container } from "react-bootstrap";

class MainPage extends Component {
  render() {
    return (
      <mainpage>
        <Container fluid>kkkk</Container>
      </mainpage>
    );
  }
}

export default MainPage;
